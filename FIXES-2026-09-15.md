# 검토 오류 수정 내역

이 문서는 REVIEW-2026-09-15.md 이후 변경 사항입니다.

## 수정

- 기존 UUID DB 스키마에 User/Drug/UserMedication/DrugFoodContraindication/Meal/MealFoodItem/MealDrugWarning 매핑 복원. 모든 모델 LIMIT 0 쿼리 성공.
- 식단·복약 라우터 등록. JWT 로그인과 실제 DB 사용자 검증 추가. 개발용 고정 사용자 접근은 명시적으로 켜야 함.
- 빈/손상/과대 이미지와 초점거리·confidence 범위 검증, 읽을 수 있는 오류 응답 추가.
- 실제 주입된 깊이 모델 → 기준 평면 → 체적 적분 → 식품별 밀도·영양 경로로 통합. 검증되지 않은 합성 성공 응답 제거.
- 체적 상한을 잘라서 정상값으로 만드는 문제 수정. NaN/무한대 거부.
- 사용자 활성 복약과 DB 규칙만 경고에 사용. 분석·음식·경고를 하나의 트랜잭션에 저장. 저장 실패 시 롤백.
- 응답 모델 연결, 점군 길이·유한값·색 범위 검사. 실제 조회 불가능한 이미지 URL 제거.
- ONNX 입력 BGR→RGB 변환, 클래스별 NMS, COCO 카테고리 이름 매핑 수정.
- 프론트 로그인, API 오류 메시지 처리 및 요청 시간 제한. GPU 고정 표시를 실제 서버 상태로 변경.
- 3D 타원체 예시 대신 응답의 점군·경계 상자 표시. 반응형 크기, WebGL 실패 메시지, GPU 자원 정리.
- 신규 DB용 초기 마이그레이션 추가. 시드는 신규 사용자 비밀번호를 환경변수로 받아 실제 해시 생성, 레코드별 누락 검사.

## 남은 외부 조건

현재 워크스페이스에는 검증된 미터 단위 깊이 모델 및 음식별 영양/밀도 데이터가 없습니다. 두 자원이 설정되기 전에는 분석이 503으로 중단됩니다. 실제 모델 정확도, 실사진 부피 오차와 임상 데이터 타당성, 실제 브라우저 WebGL 동작은 검증 완료를 주장하지 않습니다.

CUDA 시스템 라이브러리는 설치하지 않았으며 기본 추론은 CPU입니다. GPU 설정, 데이터 계약, 기존 DB 처리 및 실행 명령은 backend/README.md를 참고하세요. 기존 DB 데이터·계정 비밀번호·스키마·실행 중인 서버 프로세스는 바꾸지 않았습니다.

## 최종 검증 결과

- `RUN_DB_TESTS=1 python -m pytest tests -q`: **61 passed**. 로그인·복약 CRUD·식단 저장/조회·소유권·저장 실패 롤백 포함. 기존 Starlette/AnyIO deprecation 경고 1건은 남아 있습니다.
- `npm test`: 계약 테스트 통과.
- `npm run typecheck`: 타입 검사 통과.
- 격리된 `/tmp/volumeal-review-build`에서 `npm run build`: 운영 빌드 통과.
- `python -m compileall -q src scripts alembic`: 문법 검사 통과.
- `python -m alembic upgrade head --sql`: SQL 생성 통과. 기존 DB에 적용하지 않음.
- 깨끗한 환경의 의존성 재설치 및 실제 GPU/깊이 모델 추론은 수행하지 않음.
