import {describe,it,expect,beforeEach} from 'vitest'; import fixture from '../../../tests/fixtures/vision-estimate.json'; import type {FoodItemEstimation} from '../../types/vision'; import {correctFoodItemWeight,useMealCorrectionStore} from '../useMealCorrectionStore';
const item=fixture.data.foodItems[0] as FoodItemEstimation; describe('test_ui_correction FR-007',()=>{beforeEach(()=>useMealCorrectionStore.getState().reset());it('scales nutrients and preserves volume',()=>{const n=correctFoodItemWeight(item,item.weightG*2);expect(n.caloriesKcal).toBe(item.caloriesKcal*2);expect(n.volumeCm3).toBe(item.volumeCm3)});it('updates aggregate atomically',()=>{const s=useMealCorrectionStore.getState();s.setItems([item]);useMealCorrectionStore.getState().correctWeight(item.id,item.weightG*2);const n=useMealCorrectionStore.getState();expect(n.totalNutrition.caloriesKcal).toBe(n.items[0].caloriesKcal)});it.each([0,-1,NaN,Infinity])('rejects %s',w=>expect(correctFoodItemWeight(item,w)).toBe(item))});
