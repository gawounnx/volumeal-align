'use client';import type {FoodItemEstimation} from '../../types/vision';export default function MealCorrectionModal({item,open,disabled,onChange,onClose}:{item:FoodItemEstimation;open:boolean;disabled?:boolean;onChange:(weight:number)=>void;onClose:()=>void}){if(!open)return null;return <div role="dialog" aria-modal="true" aria-label="중량 수동 보정" className="fixed inset-0 z-40 grid place-items-center bg-black/60"><div className="rounded-xl bg-slate-900 p-5"><h2>{item.foodName} 중량 보정</h2><label>중량(g)<input type="number" min="0.01" step="0.01" defaultValue={item.weightG} disabled={disabled} onChange={e=>{const v=Number(e.target.value);if(Number.isFinite(v)&&v>0)onChange(v)}}/></label><button onClick={onClose}>완료</button></div></div>}
