'use client';import {useEffect,useState} from 'react';export default function MockProvider({children}:{children:React.ReactNode}){const [ready,setReady]=useState(process.env.NEXT_PUBLIC_API_MOCKING!=='true');useEffect(()=>{if(process.env.NEXT_PUBLIC_API_MOCKING!=='true')return;import('../../mocks/browser').then(({worker})=>worker.start({onUnhandledRequest:'bypass'})).then(()=>setReady(true));},[]);return ready?children:null}
