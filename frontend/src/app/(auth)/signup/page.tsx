'use client';import {useState} from 'react';import {z} from 'zod';import {apiClient} from '../../../lib/apiClient';const schema=z.object({email:z.string().email('올바른 이메일을 입력하세요.'),password:z.string().min(8,'비밀번호는 8자 이상이어야 합니다.')});export default function SignupPage(){const [error,setError]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const data=Object.fromEntries(new FormData(e.currentTarget));const parsed=schema.safeParse(data);if(!parsed.success)return setError(parsed.error.issues[0].message);try{await apiClient.post('/auth/signup',parsed.data);location.href='/login'}catch{setError('회원가입하지 못했습니다.')}}return <main className="mx-auto max-w-md p-8"><h1>회원가입</h1><form onSubmit={submit}><label>이메일<input name="email" type="email"/></label><label>비밀번호<input name="password" type="password"/></label><button>회원가입</button>{error&&<p role="alert">{error}</p>}</form></main>}
