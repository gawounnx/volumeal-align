'use client';import {useState} from 'react';import {z} from 'zod';import {apiClient} from '../../../lib/apiClient';const schema=z.object({email:z.string().email('올바른 이메일을 입력하세요.'),password:z.string().min(1,'비밀번호를 입력하세요.')});export default function LoginPage(){const [error,setError]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const data=Object.fromEntries(new FormData(e.currentTarget));const parsed=schema.safeParse(data);if(!parsed.success)return setError(parsed.error.issues[0].message);try{await apiClient.post('/auth/login',parsed.data);location.href='/'}catch{setError('로그인하지 못했습니다.')}}return <main className="mx-auto max-w-md p-8"><h1>로그인</h1><form onSubmit={submit}><label>이메일<input name="email" type="email"/></label><label>비밀번호<input name="password" type="password"/></label><button>로그인</button>{error&&<p role="alert">{error}</p>}</form></main>}
