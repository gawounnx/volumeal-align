import {z} from 'zod'; export const FrontendEnvironmentSchema=z.object({NODE_ENV:z.enum(['development','test','production']).default('development'),HOST:z.string().default('0.0.0.0'),PORT:z.coerce.number().int().positive().default(3002),NEXT_PUBLIC_API_BASE_URL:z.string().min(1),NEXT_PUBLIC_API_MOCKING:z.enum(['true','false']).default('false')}); export function validateFrontendEnv(v:Record<string,unknown>){return FrontendEnvironmentSchema.parse(v)}
